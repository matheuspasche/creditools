% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimization.R
\name{calculate_efficient_frontier}
\alias{calculate_efficient_frontier}
\title{Calculate the Efficient Frontier for a Credit Policy}
\usage{
calculate_efficient_frontier(data, config, score_col, ...)
}
\arguments{
\item{data}{A data frame containing applicant data.}

\item{config}{A base \code{credit_policy} object.}

\item{score_col}{The score column to optimize. (Uses \code{tidyselect} syntax).}

\item{...}{Additional arguments passed to \code{find_optimal_cutoffs()}.}
}
\value{
A tibble representing the Pareto frontier (the "Efficient Frontier").
}
\description{
A high-level wrapper that automates the process of finding optimal cutoffs
and extracting the Pareto frontier. This is a convenience function that
combines \code{find_optimal_cutoffs()} and \code{analyze_tradeoffs()}.
}
\examples{
\donttest{
data(applicants)
base_policy <- credit_policy(
  applicant_id_col = id,
  score_cols = new_score,
  current_approval_col = approved,
  actual_default_col = defaulted
)

# Calculate frontier in one step
frontier <- calculate_efficient_frontier(
  data = applicants,
  config = base_policy,
  score_col = new_score,
  cutoff_steps = 10,
  target_default_rate = 0.12,
  method = "analytical"
)

print(frontier)
}
}

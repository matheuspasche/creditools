% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stages.R
\name{stage_filter}
\alias{stage_filter}
\title{Define a Hard Filter Stage}
\usage{
stage_filter(name, condition, observed_outcome_col = NULL)
}
\arguments{
\item{name}{A character string for the name of the stage (e.g., "age_rule").}

\item{condition}{A character string representing the logical condition to be
evaluated dynamically over the applicant data. Variables used in the string
must exist in the data frame during simulation.}

\item{observed_outcome_col}{The column in the original data that contains the
observed outcome for this stage (0 or 1), if it exists.}
}
\description{
\code{stage_filter()} defines a stage where approval is strictly determined by a
logical condition string (e.g., \code{"idade >= 18"} or \code{"status == 'Válido'"}).
Applicants who evaluate to \code{FALSE} are immediately rejected from the funnel.
}
\details{
Define a Hard Filter Stage
}
\examples{
# Defines a stage that rejects anyone under 18 years old
age_rule <- stage_filter(name = "age_check", condition = "age >= 18")
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{compare_policies}
\alias{compare_policies}
\title{Compare two credit policies}
\usage{
compare_policies(sim_new, sim_old)
}
\arguments{
\item{sim_new}{A \code{credit_sim_results} object for the challenger policy.}

\item{sim_old}{A \code{credit_sim_results} object for the baseline policy.}
}
\value{
A list containing:
\itemize{
\item \code{metrics}: A tibble with global deltas (Approval Rate, Bad Rate).
\item \code{swaps}: A tibble with volume/bad rates for swap_in, swap_out, and keep_in.
\item \code{ratio}: The Swap-In to Keep-In volume ratio.
}
}
\description{
High-level business comparison between two policy simulations.
Calculates delta production, delta bad debt, and swap analytics.
}
\examples{
sim_new <- run_simulation(applicants, credit_policy(
  applicant_id_col = "id",
  score_cols = "new_score",
  current_approval_col = "approved",
  actual_default_col = "defaulted",
  simulation_stages = list(stage_cutoff("score", list(new_score = 600)))
))
sim_old <- run_simulation(applicants, credit_policy(
  applicant_id_col = "id",
  score_cols = "old_score",
  current_approval_col = "approved",
  actual_default_col = "defaulted",
  simulation_stages = list(stage_cutoff("score", list(old_score = 500)))
))
compare_policies(sim_new, sim_old)
}
\seealso{
Other analysis: 
\code{\link{run_tradeoff_analysis}()},
\code{\link{summarize_results}()}
}
\concept{analysis}

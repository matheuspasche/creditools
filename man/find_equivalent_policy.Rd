% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimization.R
\name{find_equivalent_policy}
\alias{find_equivalent_policy}
\title{Find an equivalent policy based on a target metric}
\usage{
find_equivalent_policy(
  tradeoff_results,
  target_metric = c("approval_rate", "default_rate"),
  target_value,
  tolerance = 0.01
)
}
\arguments{
\item{tradeoff_results}{Results from \code{find_optimal_cutoffs()} or \code{run_tradeoff_analysis()}.}

\item{target_metric}{The metric to match: \code{"approval_rate"} or \code{"default_rate"}.}

\item{target_value}{The numeric value to search for (e.g., 0.20 for 20\% approval).}

\item{tolerance}{The numeric tolerance for matching. Default is 0.01 (1\%).}
}
\value{
A data frame containing the closest matching scenarios.
}
\description{
Searches through multi-scenario results to find a policy that matches a
specific target value for either approval rate or default rate. Useful for
"iso-approval" or "iso-bad-rate" analyses.
}
\examples{
\donttest{
# 1. Run a tradeoff analysis
data <- generate_sample_data(n_applicants = 1000)
policy <- credit_policy(
  applicant_id_col = "id",
  score_cols = "new_score",
  current_approval_col = "approved",
  actual_default_col = "defaulted"
)
tradeoff_results <- run_tradeoff_analysis(
  data = data,
  base_policy = policy,
  vary_params = list(new_score_cutoff = seq(500, 700, 50))
)

# 2. Find a policy that yields ~20\% approval rate
find_equivalent_policy(
  tradeoff_results,
  target_metric = "approval_rate",
  target_value = 0.20
)
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grouping.R
\name{find_risk_groups}
\alias{find_risk_groups}
\title{Find Stable Risk Groups (Ratings) from Multiple Scores}
\usage{
find_risk_groups(
  data,
  score_cols,
  default_col,
  time_col = NULL,
  min_vol_ratio = 0.05,
  max_crossings = 1L,
  bins = 20,
  oot_date = NULL,
  max_groups = NULL,
  quiet = FALSE,
  optimization_method = c("ward", "iv"),
  lambda_cross = 0.5,
  lambda_vol = 0.2,
  ...
)
}
\arguments{
\item{data}{A data frame containing the historical applicant data.}

\item{score_cols}{A character vector containing the names of the score columns to matrix.}

\item{default_col}{A character string with the column name indicating the observed default (0 or 1).}

\item{time_col}{A character string with the column name representing the vintage/time cohort (e.g., "YYYY-MM"). It will be internally coerced to Date.}

\item{min_vol_ratio}{The minimum acceptable population percentage for a risk group. Groups below this threshold are merged with their nearest risk neighbor. Default is 0.05 (5\%).}

\item{max_crossings}{Maximum number of vintage periods where an adjacent lower-risk group can have a HIGHER observed PD than the next group (crossing). Uses absolute count, not proportion - so it is robust to small vintage windows (6-18 months). Default is \code{1}, meaning at most 1 month of inversion is tolerated before forcing a merge.}

\item{bins}{An integer defining the granularity of the initial matrix grid before pruning. E.g., \code{bins = 20} slices each score into 20 tiles (every 5 percentiles).}

\item{oot_date}{An optional cutoff Date/POSIXt object. Data where \code{time_col >= oot_date} will be preserved exclusively for Out-Of-Time (OOT) validation reporting.}

\item{max_groups}{An optional integer specifying the maximum number of risk groups to return. If NULL, pruning is driven solely by volume and stability thresholds.}

\item{quiet}{Whether to suppress progress and status messages. Default is FALSE.}

\item{optimization_method}{The clustering algorithm to use: \code{"ward"} (default, ultra-stable) or \code{"iv"} (experimental, maximizes Information Value).}

\item{lambda_cross}{Penalty weight for vintage PD crossings (only used if \code{optimization_method = "iv"}). Default is 0.5.}

\item{lambda_vol}{Penalty weight for PD volatility over time (only used if \code{optimization_method = "iv"}). Default is 0.2.}

\item{...}{Additional options passed to internal functions (e.g., \code{parallel = TRUE}, \code{n_workers = 4}).}
}
\value{
An object of class \code{credit_risk_groups}, which is a list containing:
\itemize{
\item \code{data}: The original data frame with a new column \code{risk_rating} attached.
\item \code{mapping}: A lookup table defining the boundaries of the final merged risk groups.
\item \code{recipes}: A list of quantile boundaries for each score used, enabling \code{predict()}.
\item \code{report}: A summary tibble validating the monotonicity and volume of the final hierarchy in BOTH train and OOT periods.
\item \code{metadata}: Internal parameters used during training.
}
}
\description{
\code{find_risk_groups()} is a powerful analytical engine designed to create a unified,
stable risk rating from one or multiple challenger scores. It automatically mattresses
the scores, generates a 1D risk ranking based on expected defaults, and applies
statistical pruning algorithms (Volume Pruning and Non-Crossing Stability Pruning)
to merge statistically insignificant or overlapping bands across time (vintages).
}
\details{
\subsection{Longitudinal Clustering (Temporal Stability)}{

The "Heart" of \code{creditools} is ensuring that risk groups remain stable and ordered over time.
When \code{time_col} is provided, the clustering engine (Ward or IV) does not just look at global PDs.
Instead, it treats each potential group as a longitudinal vector of PDs across vintages.
\enumerate{
\item \strong{Ward Method (Stability)}: Minimizes the Euclidean distance between the PD curves of
adjacent groups. Two groups with similar global PDs but different behavior in crisis
periods will be kept separate.
\item \strong{PD Crossings}: The \code{max_crossings} parameter acts as a hard constraint. If two groups
invert their risk ordering (e.g., Group 1 has higher PD than Group 2) in more than
\code{max_crossings} months, the engine will force a merge to ensure reliable decisioning.
}
}
}
\examples{
\donttest{
# Use the built-in applicants dataset
data(applicants)
groups <- find_risk_groups(
    data = applicants,
    score_cols = "old_score",
    default_col = "defaulted",
    time_col = "vintage",
    max_groups = 5
)

# View the distribution of PD across groups
print(groups$report)

# Visualize the group stability over time
plot(groups)
}
}

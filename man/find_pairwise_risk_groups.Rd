% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grouping.R
\name{find_pairwise_risk_groups}
\alias{find_pairwise_risk_groups}
\title{Pairwise Matrixing of Challengers vs Primary Score}
\usage{
find_pairwise_risk_groups(
  data,
  primary_score,
  challenger_scores,
  default_col,
  time_col,
  ...
)
}
\arguments{
\item{data}{A data frame containing the historical applicant data.}

\item{primary_score}{A character string. The baseline/legacy score.}

\item{challenger_scores}{A character vector containing the names of the challenger score columns.}

\item{default_col}{A character string with the column name indicating the observed default (0 or 1).}

\item{time_col}{A character string with the column name representing the vintage/time cohort.}

\item{...}{Additional arguments passed natively to \code{find_risk_groups()}.}
}
\value{
A named list where each element contains the clustered Output List from \code{find_risk_groups} tied to a specific Challenger.
}
\description{
\code{find_pairwise_risk_groups()} is a convenience wrapper that tests a single \code{primary_score}
against a vector of \code{challenger_scores}. Instead of attempting a massive N-Dimensional
cross (which geometrically blows up for 3+ scores), it iterates 1x1 (primary vs challenger 1,
primary vs challenger 2...) and generates parallel Risk Groups. Includes a progress bar mapping
the heavy algorithmic lifting natively.
}

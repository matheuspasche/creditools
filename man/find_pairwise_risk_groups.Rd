% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grouping.R
\name{find_pairwise_risk_groups}
\alias{find_pairwise_risk_groups}
\title{Pairwise Matrixing of Challengers vs Primary Score}
\usage{
find_pairwise_risk_groups(
  data,
  primary_score,
  challenger_scores,
  default_col,
  time_col,
  quiet = FALSE,
  optimization_method = c("ward", "iv"),
  ...
)
}
\arguments{
\item{data}{A data frame containing the historical applicant data.}

\item{primary_score}{Primary/legacy score column. (Uses \code{tidyselect} syntax).}

\item{challenger_scores}{Challenger score columns. (Uses \code{tidyselect} syntax).}

\item{default_col}{Column name indicating the observed default (0 or 1). (Uses \code{tidyselect} syntax).}

\item{time_col}{Column name representing the vintage/time cohort. (Uses \code{tidyselect} syntax).}

\item{quiet}{Whether to suppress progress and status messages. Default is FALSE.}

\item{optimization_method}{The clustering algorithm to use: \code{"ward"} or \code{"iv"}.}

\item{...}{Additional arguments passed natively to \code{find_risk_groups()} (e.g., \code{parallel = TRUE}).}
}
\value{
A named list where each element contains the clustered Output List from \code{find_risk_groups} tied to a specific Challenger.
}
\description{
\code{find_pairwise_risk_groups()} is a convenience wrapper that tests a single \code{primary_score}
against a vector of \code{challenger_scores}. Instead of attempting a massive N-Dimensional
cross (which geometrically blows up for 3+ scores), it iterates 1x1 (primary vs challenger 1,
primary vs challenger 2...) and generates parallel Risk Groups. Includes a progress bar mapping
the heavy algorithmic lifting natively.
}
\examples{
\donttest{
data(applicants)
results <- find_pairwise_risk_groups(
    data = applicants,
    primary_score = old_score,
    challenger_scores = starts_with("new_"),
    default_col = defaulted,
    time_col = vintage,
    max_groups = 3
)
}
}
